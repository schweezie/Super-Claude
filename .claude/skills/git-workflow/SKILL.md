<!-- TODO: Branch, commit, PR management skill for the SHIP phase.
     YAML frontmatter: name, description, pipeline phase, handoff target.
     Manages atomic commits, branch strategy, PR creation.
     Enforces conventional commit messages and clean git history.
     Pipeline handoff: git-workflow → release
     Used by: release-manager agent via /ship command -->
