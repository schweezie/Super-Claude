<!-- TODO: Socratic questioning skill for the IDEA phase.
     YAML frontmatter: name, description, pipeline phase, handoff target.
     Measures clarity across weighted dimensions (problem, users, scope,
     constraints, success criteria). Exposes hidden assumptions.
     Pipeline handoff: deep-interview → prd-generator
     Used by: interviewer agent via /idea command -->
