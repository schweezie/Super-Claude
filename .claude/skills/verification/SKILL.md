<!-- TODO: Output verification loops skill (cross-phase).
     YAML frontmatter: name, description, pipeline phase, handoff target.
     Verifies phase outputs against output-spec.md requirements.
     Runs coverage checks, acceptance criteria mapping, quality gates.
     Used in BUILD and TEST phases.
     Pipeline handoff: verification → git-workflow (after test)
     Used by: executor agent, tester agent -->
