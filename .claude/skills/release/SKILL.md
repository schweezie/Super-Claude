<!-- TODO: Versioning and deployment skill for the SHIP phase.
     YAML frontmatter: name, description, pipeline phase, handoff target.
     Runs release checklist, tags version, generates changelog,
     creates release PR or deploys. Final skill in the pipeline.
     Pipeline handoff: release → (end of pipeline)
     Used by: release-manager agent via /ship command -->
