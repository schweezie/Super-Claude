<!-- TODO: Multi-agent parallel execution skill for the BUILD phase.
     YAML frontmatter: name, description, pipeline phase, handoff target.
     Decomposes architecture into parallelizable work units.
     Configures Agent Teams or subagents for concurrent implementation.
     Ralph-style loop ensures completion.
     Pipeline handoff: parallel-build → tdd
     Used by: executor agent via /build command -->
