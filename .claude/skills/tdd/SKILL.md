<!-- TODO: Test-driven development skill for the TEST phase.
     YAML frontmatter: name, description, pipeline phase, handoff target.
     Generates test strategy from PRD acceptance criteria.
     Writes unit, integration, and e2e tests. Runs tests.
     Loops until all acceptance criteria have passing tests.
     Pipeline handoff: tdd → verification
     Used by: tester agent via /test command -->
