<!-- TODO: PRD creation skill for the PLAN phase.
     YAML frontmatter: name, description, pipeline phase, handoff target.
     Takes interview output (idea-brief.md), generates full PRD with
     user stories, acceptance criteria, task breakdown, dependency graph.
     Pipeline handoff: prd-generator → system-design
     Used by: planner agent via /plan command -->
