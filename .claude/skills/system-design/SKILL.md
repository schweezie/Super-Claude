<!-- TODO: Architecture design skill for the ARCHITECT phase.
     YAML frontmatter: name, description, pipeline phase, handoff target.
     Takes PRD, produces tech stack decisions, data model, API contracts,
     component diagram, infrastructure plan with justified tradeoffs.
     Pipeline handoff: system-design → parallel-build
     Used by: architect agent via /architect command -->
