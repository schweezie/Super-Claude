<!-- TODO: Code implementation agent (primary build agent).
     Phase: BUILD
     Uses parallel-build and verification skills.
     Decomposes architecture into parallelizable work units.
     Spins up Agent Teams or subagents for parallel execution.
     Model: Sonnet (code generation).
     Knowledge: agent-design.md, context-management.md, team-coordination.md -->
